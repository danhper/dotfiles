[user]
	name = Daniel Perez
	email = tuvistavie@gmail.com
	signingkey = tuvistavie@gmail.com

[push]
    default=simple

[core]
    excludesfile=/home/daniel/.gitignore_global
	pager = lv
	askPass =
[branch "master"]
	rebase = true
[alias]
	ignore = "!gi() { curl -L -s https://www.gitignore.io/api/$@ ;}; gi"
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
