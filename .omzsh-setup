ZSH=$HOME/.oh-my-zsh
COMPLETION_DIR="$HOME/.zsh-completions"

ZSH_THEME="candy"

# COMPLETION_WAITING_DOTS="true"

setopt extended_glob

plugins=(git git-flow mvn archlinux rails3 ant npm)

if [ -d $COMPLETION_DIR ]; then
  fpath+=$COMPLETION_DIR
fi

source $ZSH/oh-my-zsh.sh
