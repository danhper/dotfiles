which pyenv > /dev/null && eval "$(pyenv init -)"

[[ -f ~/.exports ]] && . ~/.exports

[[ -f ~/.connect_ssh.sh ]] && . ~/.connect_ssh.sh

[[ -f ~/.omzsh-setup ]] && . ~/.omzsh-setup

[[ -f ~/.aliases ]] && . ~/.aliases

